# pnpm v11 re-verifies deps before every command and refuses to run when
# there are ignored build scripts. Our onlyBuiltDependencies in package.json
# already declares which scripts are allowed; disabling the re-verify keeps
# the workflow snappy.
verify-deps-before-run=false
